# Copy to .env and fill in real values. .env is gitignored.
# Special characters in the password must be URL-encoded (e.g. "@" -> "%40").
DATABASE_URL=postgresql+psycopg://postgres:CHANGE_ME@localhost:5432/rewind
