# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.53.0

# --- Database ---
SQLAlchemy==2.0.54
alembic==1.20.0
# psycopg 3 (the modern successor to psycopg2). URL scheme: postgresql+psycopg://
psycopg[binary]==3.3.6

# --- Settings ---
pydantic-settings==2.15.0

# --- Auth ---
# PyJWT instead of python-jose: python-jose is effectively unmaintained and
# FastAPI's own docs switched to PyJWT.
PyJWT==2.15.0
# bcrypt used directly instead of passlib: passlib is unmaintained and breaks
# against bcrypt >= 4.1. bcrypt.hashpw / bcrypt.checkpw is all we need.
bcrypt==5.0.0

# --- Testing ---
pytest==9.1.1
httpx==0.28.1
